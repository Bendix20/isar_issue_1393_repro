name: isar_bug
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

isar_version: &isar_version 4.0.0-dev.13

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  path_provider: ^2.0.15
  isar: *isar_version
  isar_flutter_libs: *isar_version

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true
